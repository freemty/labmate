# {project-name}

> {description}

## Project knowledge

- Durable project map: `{project-skill-path}`
- Experiment index: `exp/summary.md`
- Literature landscape: `docs/papers/landscape.md`
- Active state: `.pipeline-state.json`

Load the narrowest source that answers the current question.

## Research gotchas

- Separate task, observation, intervention, baseline, metric, and success
  predicate.
- Treat single runs as observations, not general conclusions.
- Record expected outcomes before running a discriminating experiment.
- Preserve negative results and successful runs when recovering partial jobs.
- Keep `.agents/` and `.claude/` project-skill mirrors identical when both
  hosts are used.
